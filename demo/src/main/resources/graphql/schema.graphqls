type Query{
    getBooks: [Book]
    getBook(id:ID!): Book
}

type Mutation{
    createBook(book:BookInput!): Book
}

type Book{
    id: ID
    name: String
    pages: Int
}

input BookInput{
    name: String
    pages: Int
}

